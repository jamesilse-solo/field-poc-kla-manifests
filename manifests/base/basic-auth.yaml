# API key secret kept for reference (no longer applied to UI routes)
apiVersion: v1
kind: Secret
metadata:
  name: ui-admin-key
  namespace: agentgateway-system
  labels:
    api-key-group: ui-access
type: extauth.solo.io/apikey
stringData:
  api-key: agw-demo-2026
  x-user-id: admin
